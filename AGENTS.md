## Agent skills

### Issue tracker

Issues are tracked locally as markdown files under `.scratch/`. No GitHub remote is configured yet — once a GitHub remote is added, switch to GitHub Issues. See `docs/agents/issue-tracker.md`.

### Triage labels

All five canonical roles use default label names. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context layout — one `CONTEXT.md` + `docs/adr/` at the repo root. See `docs/agents/domain.md`.
